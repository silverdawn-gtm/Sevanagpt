# SevanaGPT Frontend — Environment Variables
# Copy to .env.local and adjust if your backend runs elsewhere.

# Base URL of the backend API (include the /api/v1 suffix)
NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1
