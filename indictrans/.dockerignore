# Secrets — never bake into the image
.env
.env.*
!.env.example

# Model cache (mounted as a Docker volume at runtime)
models/
adapters/

# Python
venv/
.venv/
__pycache__/
*.py[cod]

# VCS / IDE / OS
.git/
.gitignore
.DS_Store
Thumbs.db
